export class AccordionItem {

    // this shortcut creates the member variables
    // and does the assignments.
    constructor(public title: string, public text: string, public expanded: boolean) {

    }

    /*
    public title: string;
    public text: string;
    public expanded: boolean;

    constructor(title: string, text: string, expanded: boolean) {

        this.title = title;
        this.text = text;
        this.expanded = expanded;

    }
    */
}